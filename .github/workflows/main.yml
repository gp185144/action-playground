name: test-main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: First step
        run: |
          echo yay
          
      - name: Fail
        id: fail
        run: |
          throw "This is an exception message."
          
      - name: Succ
        if: ${{ always() }}
        run: |
          echo ${{ steps.fail.conclusion }}
          
      - name: Succ2
        if: ${{ always() }}
        run: |
          echo ${{ steps.fail.conclusion }}
          
#       - name: Result
#         run: |
#           cat tests-status.json
