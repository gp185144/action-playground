name: test-main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Test1
        id: test1
        continue-on-error: true
        run: |
          throw "This is an exception message."
          
      - name: Test2
        id: test2
        run: |
          echo lol
                    
      - name: Result
        run: |
          echo [ >> tests-status.json
          echo "{\"test1\": ${{ steps.test1.outcome }}}," >> tests-status.json
          echo "{\"test2\": ${{ steps.test2.outcome }}}," >> tests-status.json
          echo ] >> tests-status.json
          cat tests-status.json
