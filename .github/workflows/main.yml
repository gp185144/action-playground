name: test-main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: First step
        run: |
          echo yay
          
      - name: Succ
        run: |
          echo ${{ job.status }}
          
#       - name: Result
#         run: |
#           cat tests-status.json
