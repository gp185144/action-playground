name: test-main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: First step
        run: |
          echo yay
          
      - name: Succ
        if: ${{ success() }}
        run: |
          echo yay
          
      - name: Result
        run: |
          cat tests-status.json
